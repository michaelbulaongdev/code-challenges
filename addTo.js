function addTo(x) {

let sum = 0

for (let i=0; i<=x; i++) {
  sum = sum + i
}

return sum

}

//a function that accepts a number as a parameter and adds all natural numbers smaller or equal than the parameter
